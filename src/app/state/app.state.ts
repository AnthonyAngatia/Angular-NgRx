// import {ProductState} from '../products/state/product.reducer';

export interface State {
  // products: ProductState;// We shouldn't put product state here coz the product module is lazy loaded
  user: any;
}
